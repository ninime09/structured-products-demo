# 模型网关配置。复制成 .env 后填入，.env 已在 .gitignore 里，不会进版本库也不会进 bundle。
# ⚠ 不要把真实 key 填在这个文件里——本文件是模板，会进版本库。
# 注意：变量名不带 VITE_ 前缀是刻意的——带前缀会被 Vite 注入到客户端。

DEEPSEEK_API_KEY=
# OPENAI_API_KEY=

# 可选，默认 deepseek
# LLM_PROVIDER=deepseek
